const MS = 60;

if (MS  < 40)
{
    console.log("You need to work hard");
}
else 
{
    console.log("You cleared the exam. Great!");
}

(MS < 40) ? console.log("You need to work hard") :  console.log("you cleared the exam grade");

const result = MS < 40 ? "You need to work hard" : "You cleared the exam"

